model:
  checkpoint: /mnt/sfs_turbo/fyy/checkpoints/vjepa2/vjepa2_1_vitG_384.pt
  text_backend: t5
  text_model: google-t5/t5-large
  text_local_files_only: true

data:
  root: /mnt/sfs_turbo/rl/InternData-A1/sim
  backend: lerobot_v21

train:
  run_name: interndata_a1_stage2_action
  output_root: outputs
  stage: action_warmup
  max_steps: 10000
  batch_size_per_gpu: 1
  grad_accum_steps: 8
  num_workers: 4
  new_module_lr: 3.0e-4
  log_every: 10
  save_every: 1000
  bf16: true
  init_student: null
